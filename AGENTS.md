# Project Instructions

## Lint And Format

- Preserve the existing semicolon-free style used by the source files.
- Keep code compatible with the current Babel, Jest, and Webpack 4 toolchain.
- Prefer focused changes and update tests when runtime behavior changes.

## Session Memory Protocol

- Read `.agents/memory/rules.md`, `.agents/memory/patterns.md`, and `.agents/memory/decisions.md` before code edits.
- Propose memory updates after completing code changes and wait for explicit confirmation before writing them.
- Keep memory entries concise, deduplicated, and actionable.
